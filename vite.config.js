import { defineConfig } from 'vite';

export default defineConfig({
  // Use default root (project root) and default publicDir ('public')
  // Remove custom root and publicDir for standard Vite static site setup
});